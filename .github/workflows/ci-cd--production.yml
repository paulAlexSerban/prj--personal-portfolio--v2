name: CI/CD - PRODUCTION

on:
  workflow_dispatch:

# Sets the GITHUB_TOKEN permissions to allow deployment to GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: "production"
  cancel-in-progress: true

env:
  NODE_VERSION: 24.14.0
  FE_NEXTJS_PROJECT_ROOT: frontend/portfolio-website
  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  SITE_URL: ${{ secrets.SITE_URL }}
  ASSETS_PATH: ${{ secrets.ASSETS_PATH }}
  SITE_HOSTNAME: ${{ secrets.SITE_HOSTNAME }}
  INDEX_NOW_API_KEY: ${{ secrets.INDEX_NOW_API_KEY }}
  S3_BUCKET_REGION: ${{ secrets.S3_BUCKET_REGION }}
  S3_CONTENT_PRODUCTION_BUCKET: ${{ secrets.S3_CONTENT_PRODUCTION_BUCKET }}
  NODE_ENV: production
  GITHUB_TOKEN: ${{ secrets.CONTENT_REPO_TOKEN }}
  CONTENT_REPO_GIT_URL: ${{ secrets.CONTENT_REPO_GIT_URL }}
  

jobs:
  deploy:
    name: "Deploy To PRODUCTION"
    runs-on: ubuntu-latest

    steps:
      - name: "📥  Checkout repository"
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: "🔍  Verify Changed Files"
        uses: tj-actions/changed-files@v47
        id: changed-frontend-portfolio-website-files
        with:
          files: ${{ env.FE_NEXTJS_PROJECT_ROOT }}/**/*

      - name: "🔄  Cache node_modules"
        uses: actions/cache@v5
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: ${{ runner.os }}-node-

      - name: "🔧  Setup NodeJS ${{ env.NODE_VERSION }}"
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: '🔧 CI Deps'
        run: npm ci

      - name: "🔧  Install PROJECT dependencies"
        run: |
          npm --prefix frontend/portfolio-website install

      - name: "Build"
        run: |
          node scripts/build.js --env=prod --target-hosting=prod --dataset=live

      - name: "Package"
        run: |
          node scripts/package.js

      - name: "Configure AWS Credentials"
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.S3_BUCKET_REGION }}

      - name: "Deploy  to bucket"
        run: aws s3 sync ./package/portfolio-website/ s3://${{ secrets.S3_PRODUCTION_BUCKET }} --delete

      - name: "Invalidate  cloudfront"
        run: aws cloudfront create-invalidation --distribution-id ${{ secrets.CLOUDFRONT_PRODUCTION_DISTRIBUTION_ID }} --paths "/"
